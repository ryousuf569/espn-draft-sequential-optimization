# Tests import the model packages flatly, so conftest.py puts each package
# directory on sys.path and its own directory has to be importable too.
[pytest]
testpaths = tests
pythonpath = tests
addopts = -q --strict-markers
markers =
    needs_db: requires data/nba.sqlite, which is a release asset and not committed
